name: netlify-plugin-webmentions
inputs:
  - name: baseUrl
    description: The base url of your site
  - name: feedPath
    description: "Path to your site's feed, without a leading slash (Default: feed.xml)"
  - name: limit
    description: "Maximum number of feed entries to check (Default: 1)"
